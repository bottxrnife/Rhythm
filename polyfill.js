// Native crypto polyfill for Solana operations.
// Requires a custom dev build (npx expo run:android).
// Commented out for Expo Go compatibility — wallet signing won't work but UI is fully functional.
// To enable: uncomment the lines below and run a native build.
//
// import { install } from 'react-native-quick-crypto';
// install();
