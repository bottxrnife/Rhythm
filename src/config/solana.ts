export type SolanaCluster = 'devnet';

// Rhythm uses devnet only (never mainnet).
export const SOLANA_CLUSTER: SolanaCluster = 'devnet';

// Wallet Adapter chain (aligned with SOLANA_CLUSTER).
export const SOLANA_WALLET_CHAIN: 'solana:devnet' = 'solana:devnet';

export const SOLANA_EXPLORER_CLUSTER_PARAM: 'devnet' = 'devnet';

export const SOLANA_RPC_URL: string = 'https://api.devnet.solana.com';

